MVP:
1. Configuration of component - Version, Drop-in, Card, etc...
2. Configuration of intial call (session, paymentMethods)
3. Payment Method configuration JSON object passthrough
4. CSS override box
5. "API Explorer" box - API Call and component configuration


Open Items:
1. Add style component to EditorBar
2. Add state component to EditorBar
3. Fix redirect bug 
4. Add loading tab
5. Add Global Error component
6. Clean up backend (ie remove unused code)
7. Add documentation for initial MVP payment methods
8. Add import functionality, so that you may import a configuration
9. Update home page to include relevant and specific documentation. For example, summarize steps
10. Create tests for QA
11. Add default values or config, and explain potential errors if you misconfigure.
12. Launch fast and iterate messaging
13. (done) There is a bug when you double click a tab it will set the txvariant as undefined
14. Hook onto Adyen xhr call and log them under APIs with response code and dropdown to inspect the logs
15. Add MUI stretch component and add more transition styling to color
16. (fixed) We are sending another session request each time we modify the css. 